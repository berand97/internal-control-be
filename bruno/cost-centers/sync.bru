meta {
  name: sync
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/cost-centers/sync
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  file: @file(./centros.csv)
}
