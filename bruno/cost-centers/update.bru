meta {
  name: update
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/cost-centers/{{costCenterId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Bienestar Universitario — sede Medellín"
  }
}
