meta {
  name: reconcile approve
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/inventories/{{inventoryId}}/reconcile/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
