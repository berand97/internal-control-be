meta {
  name: reset password
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "{{resetToken}}",
    "newPassword": "Nueva-Clave!2026"
  }
}
