meta {
  name: update
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/campus/{{campusId}}/buildings/{{buildingId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "floorsCount": 5
  }
}
