meta {
  name: update
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/campus/{{campusId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "address": "Carrera 84 No. 33AA-1, Laureles"
  }
}
