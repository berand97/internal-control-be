meta {
  name: delete
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/categories/{{categoryId}}/dynamic-fields/{{dynamicFieldId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  Falla con DYNAMIC_FIELD_IN_USE si ya hay valores en asset_custom_value.
}
